services:
  qemu:
    container_name: qemu
    build: ..
    image: tailnet-qemu:0.0.1-beta
    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/kvm
      - /dev/net/tun
      - /dev/vhost-net
    environment:
      - BOOT=proxmox
