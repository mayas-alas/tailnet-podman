# Ensure /var/lib/nix backing directory exists at boot.
# This directory is bind-mounted to /nix by nix.mount when the nix sysext
# is active. Creating it here (in the base image) ensures it exists before
# the mount unit runs, even on first boot.
d /var/lib/nix 0755 root root - -
